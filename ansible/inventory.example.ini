# Copy to inventory.ini and change the host/user.
# Run from your workstation:
#   ansible-playbook -i ansible/inventory.ini ansible/playbooks/clean-ubuntu.yml --ask-become-pass

[vast_hosts]
# 192.0.2.10 ansible_user=ubuntu
# my-rig.local ansible_user=ubuntu
